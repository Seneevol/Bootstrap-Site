{{!--
    LOSTPASSWORD RESET
--}}
<section id="lostPasswordReset">
    <div class="container">
        <div class="row">
            <div class="resetPassword-title">
                <h2>Réinitialiser votre mot de passe</h2>
            </div>
            <form action="/resetPass/?_method=PUT" method="post" class="connection d-flex align-items-center flex-column">
                <div class="mb-3 element-form">
                    <label for="password" class="form-label">Nouveau mot de passe</label>
                    <input required type="password" class="form-control" id="password" name="password" onkeyup="check();">
                </div>
                <div class="mb-3 element-form">
                    <label for="password" class="form-label">Confirmation du mot de passe</label>
                    <input required type="password" class="form-control" id="password-confirmation" name="confirmation" onkeyup="check();">
                    <span id="message"></span>
                </div>
                <button type="submit" class="btn btn-connect w-45" id="submit">Changer de mot de passe</button>
            </form>
        </div>
    </div>
</section>

{{> footer}}
